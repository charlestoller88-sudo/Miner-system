# 矿机扫描异常排查指南

## 一、问题现象

- 扫描显示大量矿机「无算力」或「0 算力」
- 实际通过矿机 Web 后台查看，矿机运行正常
- 不同矿机使用不同固件（原厂/BraiinsOS/Vnish 等）

## 二、原因分析

### 1. 固件差异导致 API 响应格式不同

系统通过 **JSON-RPC（端口 4028）** 的 `summary` 命令获取算力。不同固件返回的字段名可能不同：

| 固件类型 | 常用算力字段 | 说明 |
|---------|-------------|------|
| 原厂 BMMiner | `GHS av` | 标准格式，单位 GH/s |
| 部分定制固件 | `GHS 5s`、`GHS 15m` | 不同时间窗口平均 |
| 老版本固件 | `MHS av` | 单位 MH/s |
| 部分第三方固件 | `Hashrate`、`hashrate` | 自定义字段名 |
| BraiinsOS 等 | REST API（非 4028） | 协议不同，需单独适配 |

若程序只解析 `GHS av`，而固件返回的是其他字段，就会得到 0 算力。

### 2. STATUS 码差异

部分固件对 `STATUS` 的返回与标准不同，导致整条响应被丢弃。

### 3. 网络与并发

- 大批量（如 64 台）并发扫描时，网络或矿机可能响应变慢或超时
- 超时后会被当作「无响应」或「离线」

---

## 三、已做的代码优化

1. **多字段兼容**：同时解析 `GHS av`、`GHS 5s`、`GHS 15m`、`MHS av`、`Hashrate` 等
2. **放宽 STATUS 校验**：仅在明确错误（`E`）时丢弃，其他情况尝试解析
3. **DEVS 备用**：若 `summary` 无算力，则从 `devs` 命令汇总各算力板数据
4. **诊断脚本**：可查看问题矿机的原始 API 返回，便于进一步排查

---

## 四、排查步骤

### 步骤 1：运行诊断脚本

在 Miner_system 项目根目录执行：

```bash
python scripts/diagnose_miner_api.py 10.102.1.64 10.102.1.58 10.102.1.59 10.102.1.60
```

脚本会输出每台矿机的 `summary` 原始结构和算力相关字段。

### 步骤 2：根据输出判断

- **有「算力字段」**：当前版本应已支持；若仍显示 0，请把诊断输出发给开发者
- **无已知算力字段**：说明固件使用非标准字段，需在 `miners/api_client_jsonrpc.py` 的 `_parse_hashrate_from_summary()` 中增加对应字段
- **连接失败/无响应**：检查网络、防火墙、4028 端口是否可达

### 步骤 3：降低扫描并发（若怀疑网络问题）

编辑 `config.py`：

```python
SCAN_CONFIG = {
    'batch_size': 10,   # 从 20 降到 10
    ...
}
IP_SCAN_CONFIG = {
    'concurrency': 10,  # 从 20 降到 10
    'timeout': 8,       # 增加超时到 8 秒
    ...
}
```

### 步骤 4：确认固件类型

在矿机 Web 界面（一般为 `http://矿机IP`）中查看：

- 固件名称（原厂 / Braiins OS / Vnish / 其他）
- 是否使用 4028 端口（部分固件可能关闭或改用其他端口）

若使用 **BraiinsOS** 等仅提供 REST API 的固件，需要在系统中增加对应的 HTTP API 客户端，而非 JSON-RPC。

---

## 五、新增非标准字段

若诊断脚本发现固件使用了新的算力字段（例如 `MyHashrate`），可在  
`miners/api_client_jsonrpc.py` 的 `_parse_hashrate_from_summary()` 中增加：

```python
candidates = [
    # ... 现有字段 ...
    ("MyHashrate", 1000),   # 若单位是 GH/s，除数 1000
    ("MyHashrate", 1000000), # 若单位是 MH/s，除数 1000000
]
```

修改后重新运行扫描或诊断脚本验证。
